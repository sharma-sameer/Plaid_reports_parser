from .get_item_id import extract_itemid, logger

# Run the code to create csv with [acap_id, item_id(s)] pairs.
logger.info("Run the code to create csv with [acap_id, item_id(s)] pairs.")
extract_itemid()
